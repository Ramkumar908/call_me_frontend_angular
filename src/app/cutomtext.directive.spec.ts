import { CutomtextDirective } from './cutomtext.directive';

describe('CutomtextDirective', () => {
  it('should create an instance', () => {
    const directive = new CutomtextDirective();
    expect(directive).toBeTruthy();
  });
});
